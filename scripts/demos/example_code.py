"""Example code with intentional style issues for testing agents."""


def badly_formatted(x, y, z):
    """Add three numbers together."""
    result = x + y + z
    return result


def another_function():
    """Calculate sum of two additions."""
    x = 1 + 2
    y = 3 + 4
    return x + y


class MyClass:
    """Example class for demonstration."""

    def __init__(self, name, age):
        """Initialize with name and age."""
        self.name = name
        self.age = age

    def get_info(self):
        """Get formatted information."""
        return f"{self.name} is {self.age} years old"
